
        //Main Logic
        switch (filtered_input) {
            case 'clear':
                $('#output').text('');
                break;
            case 'help':
                wrt('whoami --> this command reveals your ip address and device name\nclear --> this command clears the console\nprojects --> this command prints and launches all my projects, can be specific if used with --project_number [value]')
                break;
            case 'whoami':
                wrt('Loading...');
                setTimeout(wrt('IPv4 : '+ip4.responseJSON.ip +'\nBrowser : '+navigator.userAgentData.brands[0].brand + ' Version '+navigator.userAgentData.brands[0].version+'\nAccessing Using Mobile : '+navigator.userAgentData.mobile+'\nBase API : '+navigator.platform+'\nDevice Memory [in GB] : '+navigator.deviceMemory+'\n'),2000);
                
                break;
            default:
                $('#output').append('\n"'+filtered_input+'" is not recognized as an command. Please use "help" to get a list of commands.');

        }
